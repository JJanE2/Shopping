<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.my.shopping.mapper.ProductMapper">

    <insert id="insert" parameterType="productCreateDto">

        <selectKey keyProperty="id" resultType="java.lang.Long" order="BEFORE">
            SELECT product_seq.NEXTVAL FROM dual
        </selectKey>

        INSERT INTO product (id, memberId, name, price, description, stockQuantity)
        VALUES (#{id}, #{memberId}, #{name}, #{price}, #{description}, #{stockQuantity})
    </insert>

    <select id="findById" resultType="Product">
        SELECT * FROM PRODUCT
        WHERE
            id = #{id}
    </select>

    <select id="findByName" resultType="Product">
        SELECT * FROM PRODUCT
        WHERE
            name = #{name}
    </select>

    <update id="update" parameterType="productUpdateDto">
        UPDATE product
        SET
            name = #{name},
            price = #{price},
            description = #{description},
            stockQuantity = #{stockQuantity}
        WHERE
            id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM PRODUCT
        WHERE
            id = #{id}
    </delete>

    <select id="findByMemberId" resultType="Product">
        SELECT * FROM PRODUCT
        WHERE
            memberId = #{memberId}
    </select>

    <update id="decreaseStock">
        UPDATE product
        SET
            stockQuantity = stockQuantity - #{quantity}
        WHERE
            id = #{productId}
            AND stockQuantity >= #{quantity}
    </update>

    <select id="findAll" resultType="Product">
        SELECT * FROM PRODUCT
        ORDER BY
            createdAt DESC
    </select>
</mapper>